from pathlib import Path

ROOT = Path(__file__).parent.parent
DIR_FETCH = ROOT / "fetch"
DIR_TOKENIZES = ROOT / "tokenize"
DIR_TRANS = ROOT / "trans"
DIR_SOURCE = ROOT / "source"
DIR_OUTPUT = ROOT / "output"

